# Don't commit the python refuse
env
__pycache__

# Don't commit the config
config.toml

# Don't include log files
*.log

# Avoid including any intermediate data downloads 
*.json
*.csv
*.parquet
*.parquet.gzip

# Any csv or json in a 'conf' folder is fair game
!**/conf/*.json
!**/conf/*.csv

# The logging config is okay
!logging_config.json
